{
  "name": "mud",
  "version": "1.0.0",
  "description": "a game (multi user dungeon)",
  "main": "index.js",
  "scripts": {
    "bundle-js": "browserify lib/hello.js > static.js",
    "build-js": "babel src -d lib --source-maps",
    "build":"npm run build-js && npm run bundle-js"

  },
  "keywords": [
    "es",
    "2015",
    "demo"
  ],
  "author": "Elsis Sitati",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "browserify": "^14.3.0"
  }
}
